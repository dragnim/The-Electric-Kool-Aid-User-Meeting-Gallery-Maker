# User settings — machine-specific, contains local paths
.umip_settings.json
~/.umip_settings.json

# Crash log — generated by the app on errors
crash-log.txt

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Processed output folders (optional — remove this line if you want to
# commit processed images to the repo)
processed/
